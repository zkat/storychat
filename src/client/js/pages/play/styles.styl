html,body { height: 100%; margin: 0; padding: 0; }
play-page {
  display: block;
  position: relative;
  width: 600px;
  height: 100%;
  margin: 0 auto;
  chat-output {
    max-height: 100%;
    margin-bottom: 150px;
  }
  chat-input {
    position: absolute;
    bottom: 0;
    border-top: 1px solid black;
    width: 600px;
    height: 150px;
  }
}