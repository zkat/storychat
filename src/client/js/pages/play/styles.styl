play-page {
  margin: 0 auto;
  width: 33em;
  position: relative;
  .user-list-container {
    position: relative;
    font-size: 0.8em;
    .user-list {
      position: fixed;
      top: 8em;
      left: 2em;
      padding: 1em;
      background-color: lightgrey;
      list-style: none;
    }
  }
}